name: quantum-demo
channels:
  - conda-forge
dependencies:
  - python=3.10
  - appmode
  - pip
  - pip:
      - amplify>=0.10.4, <1.0.0
      - dwave-system>=1.3.0
      - minorminer>=0.2.5
      - japanmap
      - matplotlib
      - numpy
      - ipywidgets
      - geopy
      - folium
      - pandas
      - sklearn
      - torch
      - scikit-learn
      - ipympl
      - sigfig
