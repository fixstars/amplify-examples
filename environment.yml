name: quantum-demo
channels:
  - conda-forge
dependencies:
  - appmode
  - pip
  - pip:
      - amplify>=0.10.4
      - dwave-system>=1.3.0
      - minorminer>=0.2.5
      - japanmap
      - matplotlib
      - numpy
      - ipywidgets
      - geopy
      - folium
      - pandas
      - sklearn
