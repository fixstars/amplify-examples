workflow:
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
    - if: "$CI_COMMIT_BRANCH && $CI_OPEN_MERGE_REQUESTS"
      when: never
    - if: "$CI_COMMIT_BRANCH || $CI_COMMIT_TAG"

stages:
  - prepare
  - test
  - format
  - output
  - push

.python-base:
  image: ${CI_REGISTRY_IMAGE}:${CI_COMMIT_REF_SLUG}
  tags:
    - docker
  cache:
    key:
      files:
        - pyproject.toml
    paths:
      - .venv
  before_script:
    - uv sync

variables:
  GIT_SUBMODULE_STRATEGY: recursive
  BASE_PYTHON_VER: "3.10"
  UV_CACHE_DIR: $CI_PROJECT_DIR/.cache/uv

.git-scripts:
  setup-config:
    - git remote set-url origin https://gitlab-ci-token:$GITLAB_API_ACCESS_TOKEN@${CI_REPOSITORY_URL##*@}
    - git config user.email "noreply@fixstars.com"
    - git config user.name "GitLab Bot"

  checkout-current:
    - git fetch origin $CI_COMMIT_REF_NAME
    - git switch -C $CI_COMMIT_REF_NAME origin/$CI_COMMIT_REF_NAME
    - git clean -xfd

build docker:
  stage: prepare
  resource_group: docker build
  tags:
    - shell
    - epyc128
  script:
    - docker system prune -f --filter "until=720h" --filter "label=repository=${CI_PROJECT_URL}"
    - docker buildx build -t ${CI_REGISTRY_IMAGE}:${CI_COMMIT_REF_SLUG} .

execute changes:
  extends: .python-base
  stage: test
  tags:
    - docker
    - epyc128
  script:
    - |-
      for NBPATH in $(git diff --name-only HEAD^..HEAD | grep '.ipynb' | grep -v 'demo' || true); do
          echo "[NbConvertApp] Executing notebook $NBPATH"
          OUTPUT=$(uv run jupyter nbconvert --to html --stdout --execute "$NBPATH" --log-level WARN 2>&1 >/dev/null)
          EXIT_CODE=$?
          if [ -n "$OUTPUT" ]; then
              echo "$OUTPUT" 1>&2
          fi
          if [ $EXIT_CODE -ne 0 ]; then
              STATUS=1
          fi
      done
    - if [ -n "$STATUS" ]; then exit 1; fi
  rules:
    - changes:
        - notebooks/**/*.ipynb
    - if: $CI_COMMIT_REF_NAME == $CI_DEFAULT_BRANCH
      when: never

execute all:
  extends: .python-base
  stage: test
  tags:
    - docker
    - epyc128
  script:
    - |-
      for NBPATH in $(/bin/ls -1 notebooks/{en,ja}/{tutorials,examples}/*.ipynb); do
          echo "[NbConvertApp] Executing notebook $NBPATH"
          OUTPUT=$(uv run jupyter nbconvert --to html --stdout --execute "$NBPATH" --log-level WARN 2>&1 >/dev/null)
          EXIT_CODE=$?
          if [ -n "$OUTPUT" ]; then
              echo "$OUTPUT" 1>&2
          fi
          if [ $EXIT_CODE -ne 0 ]; then
              STATUS=1
          fi
      done
    - if [ -n "$STATUS" ]; then exit 1; fi
  rules:
    - if: $CI_COMMIT_REF_NAME == $CI_DEFAULT_BRANCH
      changes:
        - notebooks/**/*.ipynb
      when: on_success
    - if: $CI_COMMIT_REF_NAME == $CI_DEFAULT_BRANCH
      when: manual
  allow_failure: true

format:
  extends: .python-base
  stage: format
  resource_group: git-push
  tags:
    - docker
    - epyc128
  script:
    - !reference [.git-scripts, setup-config]
    - !reference [.git-scripts, checkout-current]
    - uv run ./format.sh
    - git add -u
    - |-
      if [ $(git status -s -uno --ignore-submodules=all | wc -l) -gt 0 ]; then
          git commit -m "🎨 Format"
          git push origin HEAD:$CI_COMMIT_REF_NAME
          exit 1
      fi
  rules:
    - if: $CI_COMMIT_REF_NAME == $CI_DEFAULT_BRANCH

html:
  extends: .python-base
  stage: output
  resource_group: git-push
  tags:
    - docker
    - epyc128
  script:
    - !reference [.git-scripts, setup-config]
    - !reference [.git-scripts, checkout-current]
    - uv run ./make-html.sh
    - git add -A html/
    - |-
      if [ $(git status -s -uno --ignore-submodules=all | wc -l) -gt 0 ]; then
          git commit -m "📝 Update html files"
          git push origin HEAD:$CI_COMMIT_REF_NAME
          exit 1
      fi
  rules:
    - if: $CI_COMMIT_REF_NAME == $CI_DEFAULT_BRANCH

sync-repositories:
  stage: push
  tags:
    - docker
  image: bitnami/git
  resource_group: git-push
  script:
    # prepare remote repositories
    - !reference [.git-scripts, setup-config]
    - git config remote.github.url >&- && git remote set-url github https://$GITHUB_USERNAME:$GITHUB_TOKEN@github.com/fixstars/amplify-examples.git || git remote add github https://$GITHUB_USERNAME:$GITHUB_TOKEN@github.com/fixstars/amplify-examples.git
    - git fetch github main
    - git fetch origin main develop
    - git switch -C develop origin/develop
    - git clean -xfd
    # rebase and push
    - git status
    - git rebase github/main
    - git push origin develop
    - git push origin develop:main
    - git push github develop:main
  rules:
    - if: '$CI_COMMIT_BRANCH == "main" || $CI_COMMIT_BRANCH == "develop"'
